#import Error from "../../shared.graphql"

type Query {
	getUserFollowers(userId: String): [String!]
	getUserFollowing(userId: String): [String!]
}

type Mutation {
	followUser(userId: String): [Error]
	unfollowUser(userId: String): [Error]
}
